; Copie este arquivo para secrets.cfg (gitignored) e preencha a chave.
; Alternativa: exporte a variável de ambiente PENO_API_KEY.
; No CI, o job de export gera secrets.cfg a partir do secret PENO_API_KEY.
[api]
key=""
